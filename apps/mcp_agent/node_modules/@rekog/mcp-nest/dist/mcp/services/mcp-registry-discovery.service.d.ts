import { InjectionToken, OnApplicationBootstrap } from '@nestjs/common';
import { DiscoveryService, MetadataScanner, ModulesContainer } from '@nestjs/core';
import { ToolMetadata } from '../decorators';
import { ResourceMetadata } from '../decorators/resource.decorator';
import { PromptMetadata } from '../decorators/prompt.decorator';
import { ResourceTemplateMetadata } from '../decorators/resource-template.decorator';
import type { McpOptions } from '../interfaces';
export type DiscoveredCapability<T extends object> = {
    type: 'tool' | 'resource' | 'resource-template' | 'prompt';
    metadata: T;
    providerClass: InjectionToken;
    methodName: string;
};
export type InjectionTokenWithName = InjectionToken & {
    name: string;
};
export declare class McpRegistryDiscoveryService implements OnApplicationBootstrap {
    private readonly discovery;
    private readonly metadataScanner;
    private readonly modulesContainer;
    private readonly options?;
    private readonly logger;
    private discoveredCapabilitiesByMcpModuleId;
    constructor(discovery: DiscoveryService, metadataScanner: MetadataScanner, modulesContainer: ModulesContainer, options?: McpOptions | undefined);
    onApplicationBootstrap(): void;
    private discoverTools;
    private buildServerNameToModuleIdMap;
    private collectFeatureRegistrations;
    private processFeatureRegistrations;
    private findModuleWithProviders;
    private discoverToolsFromProviders;
    private discoverToolsForModuleSubtree;
    private addDiscovery;
    private addDiscoveryPrompt;
    private addDiscoveryTool;
    private addDiscoveryResource;
    private addDiscoveryResourceTemplate;
    getMcpModuleIds(): string[];
    getTools(mcpModuleId: string): DiscoveredCapability<ToolMetadata>[];
    findTool(mcpModuleId: string, name: string): DiscoveredCapability<ToolMetadata> | undefined;
    getResources(mcpModuleId: string): DiscoveredCapability<ResourceMetadata>[];
    findResource(mcpModuleId: string, name: string): DiscoveredCapability<ResourceMetadata> | undefined;
    getResourceTemplates(mcpModuleId: string): DiscoveredCapability<ResourceTemplateMetadata>[];
    findResourceTemplate(mcpModuleId: string, name: string): DiscoveredCapability<ResourceTemplateMetadata> | undefined;
    getPrompts(mcpModuleId: string): DiscoveredCapability<PromptMetadata>[];
    findPrompt(mcpModuleId: string, name: string): DiscoveredCapability<PromptMetadata> | undefined;
    private convertTemplate;
    private extractTemplateQueryParams;
    private parseQueryString;
    private stripQueryString;
    private convertUri;
    findResourceByUri(mcpModuleId: string, uri: string): {
        resource: DiscoveredCapability<ResourceMetadata>;
        params: Record<string, string>;
    } | undefined;
    findResourceTemplateByUri(mcpModuleId: string, uri: string): {
        resourceTemplate: DiscoveredCapability<ResourceTemplateMetadata>;
        params: Record<string, string>;
    } | undefined;
    registerDynamicCapability<T extends object>(mcpModuleId: string, capability: DiscoveredCapability<T>): void;
    removeDynamicCapability(mcpModuleId: string, type: DiscoveredCapability<object>['type'], identifier: string): void;
}
//# sourceMappingURL=mcp-registry-discovery.service.d.ts.map